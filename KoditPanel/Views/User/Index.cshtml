@model List<EntityLayer.Concrete.User>
@{
    ViewData["Title"] = "Kullanıcı Listesi";
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] - RKAPanel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        body {
            min-height: 100vh;
            background-color: #f8f9fa;
        }

        /* Sol Menü */
        .sidebar {
            background-color: #343a40;
            color: white;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            width: 240px;
            padding: 20px;
        }

            .sidebar h4 {
                text-align: center;
                margin-bottom: 30px;
            }

            .sidebar a {
                display: block;
                color: white;
                text-decoration: none;
                margin-bottom: 10px;
                padding: 10px 15px;
                border-radius: 6px;
                transition: 0.2s;
            }

                .sidebar a:hover {
                    background-color: #495057;
                }

        .logout-btn {
            margin-top: 20px;
        }

        /* İçerik Alanı */
        .content {
            margin-left: 260px;
            padding: 40px;
        }

        .card {
            border: none;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            border-radius: 12px;
        }

        .table thead th {
            background-color: #212529;
            color: white;
        }

        .btn-info {
            background-color: #0dcaf0;
            border: none;
        }

            .btn-info:hover {
                background-color: #0bb5d1;
            }
    </style>
</head>
<body>

    <!-- Sol Menü -->
    <div class="sidebar">
        <h4>Admin Panel</h4>
        <a href="/User/Index"><i class="bi bi-clipboard-data me-2"></i>Kullanıcı Logları</a>
        <a href="/User/AddUser"><i class="bi bi-person-plus me-2"></i>Kullanıcı Ekle</a>
        <a href="/User/Users"><i class="bi bi-people me-2"></i>Kullanıcı Bilgileri</a>

        <form asp-controller="Login" asp-action="Logout" method="post" class="logout-btn">
            <button type="submit" class="btn btn-danger w-100">
                <i class="bi bi-box-arrow-right me-2"></i>Çıkış
            </button>
        </form>
    </div>

    <!-- İçerik -->
    <div class="content">
        <div class="card p-4">
            <h3 class="mb-4">
                <i class="bi bi-people me-2"></i>Kullanıcı Listesi
            </h3>

            <table class="table table-hover align-middle">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Ad Soyad</th>
                        <th>Kullanıcı Adı</th>
                        <th>Son Giriş</th>
                        <th>Admin mi?</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.NameSurname</td>
                                <td>@item.UserName</td>
                                <td>@item.LastLogin?.ToString("g")</td>
                                <td>@(item.IsAdmin ? "Evet" : "Hayır")</td>
                                <td>
                                    <a href="/User/LoginLogs/@item.Id" class="btn btn-info btn-sm me-1">
                                        <i class="bi bi-list-check"></i> Loglar
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="8" class="text-center text-muted">Henüz kayıtlı kullanıcı yok</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

</body>
</html>
